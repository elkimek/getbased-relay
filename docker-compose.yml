services:
  relay:
    build: .
    ports:
      - "4000:4000"
      - "4001:4001"
    volumes:
      - relay-data:/data
    environment:
      - RELAY_PORT=4000
      - ADMIN_PORT=4001
      - QUOTA_PER_OWNER_MB=10
      - QUOTA_GLOBAL_MB=1000
      - LOG_FORMAT=json
      - LOG_LEVEL=info
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:4001/health').then(r=>{if(!r.ok)throw 1}).catch(()=>process.exit(1))"]
      interval: 30s
      timeout: 5s
      retries: 3

volumes:
  relay-data:
